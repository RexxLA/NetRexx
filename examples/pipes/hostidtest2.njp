/* hostid test 2 - not first stage */

pipe (hostidtest2) literal abc | hostid | console ? literal PIPE Error: hostid_2 stage must be the first stage of a pipeline. | console ? literal PASS if above two lines are the exactly the same - FAIL otherwise | console 