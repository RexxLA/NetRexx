/* hostname test 2 - not first stage */

pipe (hostnametest2) literal abc | hostname | console ? literal PIPE Error: hostname_2 stage must be the first stage of a pipeline. | console ? literal PASS if above two lines are the exactly the same - FAIL otherwise | console 