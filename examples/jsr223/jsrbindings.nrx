import javax.script.

import javax.script.

nrEngine = ScriptEngineManager().getEngineByName("NetRexx")

nrEngine.put("one", Rexx "1")
nrEngine.put("two", Rexx "0")

nrEngine.eval('say "one =" one; two = one + 1')

say 'two = 'nrEngine.get("two")

