/* netrexx tool to download native implementation to local installation */
--trace results
-- get environment
os_name       = System.getProperty("os.name")
architecture  = System.getProperty("os.arch")
java_version  = System.getProperty("java.version")

-- compose the url


-- download the file
--Files.copy(URL("http://example.com/update/PubApp_2.0.jar").openStream(),Paths.get("C:/PubApp_2.0/update/lib/kitap.jar"))
